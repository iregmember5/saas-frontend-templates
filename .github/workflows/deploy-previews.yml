name: Deploy Template Previews

on:
  push:
    branches: [main]
    paths:
      - "templates-frontend/**"
      - ".github/workflows/deploy-previews.yml"
  workflow_dispatch:
    inputs:
      deploy_all:
        description: "Deploy all templates (not just changed)"
        required: false
        type: boolean
        default: false

jobs:
  deploy-templates:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"

      - name: Detect Templates to Deploy
        id: detect
        run: |
          echo "🔍 Detecting templates to deploy..."

          # If manual trigger with deploy_all=true, deploy everything
          if [ "${{ github.event.inputs.deploy_all }}" = "true" ]; then
            echo "📦 Manual trigger: Deploying ALL templates"
            ALL_TEMPLATES=$(ls -1 templates-frontend/ | grep -v "^\\." | tr '\n' ',' | sed 's/,$//')
            echo "templates=$ALL_TEMPLATES" >> $GITHUB_OUTPUT
            echo "deploy_all=true" >> $GITHUB_OUTPUT
            exit 0
          fi

          # Detect changed templates
          CHANGED_DIRS=$(git diff --name-only HEAD^ HEAD | grep '^templates-frontend/' | cut -d'/' -f2 | sort -u)

          if [ -z "$CHANGED_DIRS" ]; then
            echo "ℹ️  No templates changed"
            echo "templates=" >> $GITHUB_OUTPUT
            echo "deploy_all=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "📦 Changed templates:"
          echo "$CHANGED_DIRS"

          CHANGED_LIST=$(echo "$CHANGED_DIRS" | tr '\n' ',' | sed 's/,$//')
          echo "templates=$CHANGED_LIST" >> $GITHUB_OUTPUT
          echo "deploy_all=false" >> $GITHUB_OUTPUT

      - name: Check Existing Deployments on VPS
        if: steps.detect.outputs.templates == '' && steps.detect.outputs.deploy_all != 'true'
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            # Check if any previews are deployed
            if [ ! -d "/var/www/previews" ] || [ -z "$(ls -A /var/www/previews 2>/dev/null)" ]; then
              echo "NO_DEPLOYMENTS=true" > /tmp/deployment-status.txt
            else
              echo "NO_DEPLOYMENTS=false" > /tmp/deployment-status.txt
            fi

      - name: Decide Deployment Strategy
        id: strategy
        run: |
          # If no changes detected, check if we need initial deployment
          if [ -z "${{ steps.detect.outputs.templates }}" ]; then
            echo "ℹ️  No changed templates detected"
            echo "🔍 Checking if initial deployment is needed..."
            
            # Deploy all templates on first run
            ALL_TEMPLATES=$(ls -1 templates-frontend/ | grep -v "^\\." | tr '\n' ',' | sed 's/,$//')
            echo "📦 Initial deployment: Deploying ALL templates"
            echo "templates=$ALL_TEMPLATES" >> $GITHUB_OUTPUT
            echo "is_initial=true" >> $GITHUB_OUTPUT
          else
            echo "templates=${{ steps.detect.outputs.templates }}" >> $GITHUB_OUTPUT
            echo "is_initial=false" >> $GITHUB_OUTPUT
          fi

      - name: Build Templates
        if: steps.strategy.outputs.templates != ''
        env:
          TEMPLATES_TO_BUILD: ${{ steps.strategy.outputs.templates }}
        run: |
          echo "🚀 Building templates: $TEMPLATES_TO_BUILD"

          IFS=',' read -ra TEMPLATES <<< "$TEMPLATES_TO_BUILD"

          mkdir -p deployment-package

          for TEMPLATE in "${TEMPLATES[@]}"; do
            echo ""
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "📦 Processing: $TEMPLATE"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            
            TEMPLATE_PATH="templates-frontend/$TEMPLATE"
            
            if [ ! -f "$TEMPLATE_PATH/package.json" ]; then
              echo "⚠️  Skipping $TEMPLATE (not a valid React project)"
              continue
            fi
            
            echo "🔨 Building $TEMPLATE..."
            cd "$TEMPLATE_PATH"
            
            # Install dependencies
            if npm ci 2>/dev/null || npm install; then
              echo "✅ Dependencies installed"
            else
              echo "❌ Failed to install dependencies for $TEMPLATE"
              cd ../..
              continue
            fi
            
            # Build project
            if npm run build; then
              echo "✅ Build successful for $TEMPLATE"
              
              # Verify dist exists
              if [ ! -d "dist" ]; then
                echo "❌ Build completed but dist folder not found"
                cd ../..
                continue
              fi
              
              # Copy to deployment package
              mkdir -p "../../deployment-package/$TEMPLATE"
              cp -r dist "../../deployment-package/$TEMPLATE/"
              
              # Create deployment marker
              echo "TEMPLATE=$TEMPLATE" > "../../deployment-package/$TEMPLATE.env"
              echo "BUILD_SUCCESS=true" >> "../../deployment-package/$TEMPLATE.env"
              echo "BUILD_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "../../deployment-package/$TEMPLATE.env"
              
            else
              echo "❌ Build failed for $TEMPLATE"
              echo "TEMPLATE=$TEMPLATE" > "../../deployment-package/$TEMPLATE.env"
              echo "BUILD_SUCCESS=false" >> "../../deployment-package/$TEMPLATE.env"
            fi
            
            cd ../..
          done

          echo ""
          echo "✅ All builds completed!"
          echo ""
          echo "📦 Deployment package contents:"
          ls -lR deployment-package/ || echo "No deployment package created"

      - name: Upload to VPS
        if: steps.strategy.outputs.templates != ''
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "deployment-package/*"
          target: "/tmp/"
          strip_components: 0
          rm: true
          overwrite: true

      - name: Deploy on VPS
        if: steps.strategy.outputs.templates != ''
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            #!/bin/bash
            set -e

            echo "🚀 Starting VPS deployment..."

            if [ ! -d "/tmp/deployment-package" ]; then
              echo "❌ Deployment package not found!"
              exit 1
            fi

            mkdir -p /var/www/previews
            mkdir -p /var/log/template-deployments

            for ENV_FILE in /tmp/deployment-package/*.env; do
              if [ ! -f "$ENV_FILE" ]; then
                continue
              fi
              
              source "$ENV_FILE"
              
              if [ "$BUILD_SUCCESS" != "true" ]; then
                echo "⚠️  Skipping $TEMPLATE (build failed)"
                continue
              fi
              
              echo ""
              echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              echo "📦 Deploying: $TEMPLATE"
              echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              
              SUBDOMAIN=$(echo "$TEMPLATE" | sed 's/-template$//')
              SUBDOMAIN="preview-$SUBDOMAIN"
              
              MAIN_DOMAIN="${{ secrets.MAIN_DOMAIN }}"
              FULL_DOMAIN="$SUBDOMAIN.$MAIN_DOMAIN"
              DEPLOY_DIR="/var/www/previews/$SUBDOMAIN"
              
              echo "🌐 Domain: $FULL_DOMAIN"
              echo "📁 Deploy to: $DEPLOY_DIR"
              
              SOURCE_DIR="/tmp/deployment-package/$TEMPLATE/dist"
              
              if [ ! -d "$SOURCE_DIR" ]; then
                echo "❌ Source not found: $SOURCE_DIR"
                ls -la /tmp/deployment-package/$TEMPLATE/ 2>/dev/null || true
                continue
              fi
              
              echo "✅ Source verified: $SOURCE_DIR"
              
              mkdir -p "$DEPLOY_DIR"
              
              echo "📦 Copying files..."
              rsync -av --delete "$SOURCE_DIR/" "$DEPLOY_DIR/"
              
              chown -R www-data:www-data "$DEPLOY_DIR"
              chmod -R 755 "$DEPLOY_DIR"
              
              if [ ! -f "$DEPLOY_DIR/index.html" ]; then
                echo "❌ Deployment failed - index.html not found"
                continue
              fi
              
              echo "✅ Files deployed successfully"

              # Create .htaccess file for React Router
              cat > "$DEPLOY_DIR/.htaccess" << 'HTACCESS'
              # React Router configuration
              RewriteEngine On
              RewriteBase /
              
              # Don't rewrite files or directories
              RewriteCond %{REQUEST_FILENAME} -f [OR]
              RewriteCond %{REQUEST_FILENAME} -d
              RewriteRule ^ - [L]
              
              # Rewrite everything else to index.html
              RewriteRule ^ index.html [L]
              
              # MIME types (in case Apache config doesn't load)
              AddType application/javascript .js
              AddType text/css .css
              AddType image/svg+xml .svg
              AddType application/json .json
              HTACCESS
              
              chmod 644 "$DEPLOY_DIR/.htaccess"
              echo "✅ .htaccess created for React Router"
              
              echo "⚙️  Configuring Apache..."
              
              cat > "/etc/apache2/sites-available/$SUBDOMAIN.conf" <<APACHECONF
              <VirtualHost *:80>
                  ServerName $FULL_DOMAIN
                  ServerAlias www.$FULL_DOMAIN
                  DocumentRoot $DEPLOY_DIR
                  
                  <Directory $DEPLOY_DIR>
                      Options -Indexes +FollowSymLinks +MultiViews
                      AllowOverride All
                      Require all granted
                      
                      # REACT ROUTER FIX: Proper rewrite rules
                      <IfModule mod_rewrite.c>
                          RewriteEngine On
                          RewriteBase /
                          
                          # Handle static files first
                          RewriteCond %{REQUEST_FILENAME} -f [OR]
                          RewriteCond %{REQUEST_FILENAME} -d
                          RewriteRule ^ - [L]
                          
                          # Handle everything else (React Router)
                          RewriteRule ^ index.html [L]
                      </IfModule>
                      
                      # Enable .htaccess overrides
                      FallbackResource /index.html
                  </Directory>
                  
                  # CRITICAL: MIME types for React/Vite apps
                  AddType application/javascript .js
                  AddType application/javascript .mjs
                  AddType text/css .css
                  AddType image/svg+xml .svg
                  AddType application/json .json
                  AddType font/woff2 .woff2
                  AddType font/woff .woff
                  AddType font/ttf .ttf
                  
                  # Vite-specific MIME types
                  AddType text/javascript .js
                  AddType text/css .css
                  
                  # Security headers
                  Header always set X-Frame-Options "SAMEORIGIN"
                  Header always set X-Content-Type-Options "nosniff"
                  Header always set X-XSS-Protection "1; mode=block"
                  Header always set Referrer-Policy "strict-origin-when-cross-origin"
                  
                  # Compression
                  <IfModule mod_deflate.c>
                      AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css
                      AddOutputFilterByType DEFLATE application/javascript application/json
                      AddOutputFilterByType DEFLATE image/svg+xml
                  </IfModule>
                  
                  # Caching
                  <IfModule mod_expires.c>
                      ExpiresActive On
                      
                      # JavaScript and CSS
                      ExpiresByType application/javascript "access plus 1 year"
                      ExpiresByType text/css "access plus 1 year"
                      
                      # Images
                      ExpiresByType image/jpg "access plus 1 year"
                      ExpiresByType image/jpeg "access plus 1 year"
                      ExpiresByType image/gif "access plus 1 year"
                      ExpiresByType image/png "access plus 1 year"
                      ExpiresByType image/svg+xml "access plus 1 year"
                      ExpiresByType image/webp "access plus 1 year"
                      ExpiresByType image/avif "access plus 1 year"
                      
                      # Fonts
                      ExpiresByType font/woff "access plus 1 year"
                      ExpiresByType font/woff2 "access plus 1 year"
                      ExpiresByType application/font-woff "access plus 1 year"
                      
                      # HTML (no caching)
                      ExpiresByType text/html "access plus 0 seconds"
                  </IfModule>
                  
                  ErrorLog \${APACHE_LOG_DIR}/$SUBDOMAIN-error.log
                  CustomLog \${APACHE_LOG_DIR}/$SUBDOMAIN-access.log combined
              </VirtualHost>
              APACHECONF
              
              a2ensite "$SUBDOMAIN.conf" 2>/dev/null || true
              
              echo "$(date -u +%Y-%m-%dT%H:%M:%SZ) | $TEMPLATE | $FULL_DOMAIN | SUCCESS" >> /var/log/template-deployments/deployments.log
              
              echo "✅ Deployed: http://$FULL_DOMAIN"
            done

            echo ""
            echo "🔍 Testing Apache..."

            if apache2ctl configtest 2>&1 | grep -q "Syntax OK"; then
              echo "✅ Apache config valid"
              
              echo "🔄 Reloading web server..."
              if systemctl reload apache2 2>/dev/null; then
                echo "✅ Apache reloaded"
              elif systemctl reload nginx 2>/dev/null; then
                echo "✅ Nginx reloaded"
              elif service apache2 reload 2>/dev/null; then
                echo "✅ Apache reloaded via service"
              else
                echo "⚠️  Auto-reload failed (CloudPanel may be managing)"
                echo "📝 Sites deployed successfully, manual reload may be needed"
                echo "   Run: sudo systemctl reload apache2"
              fi
            else
              echo "❌ Apache config error"
              apache2ctl configtest
              exit 1
            fi

            echo ""
            echo "🔐 Installing SSL certificates..."

            for ENV_FILE in /tmp/deployment-package/*.env; do
              [ ! -f "$ENV_FILE" ] && continue
              source "$ENV_FILE"
              [ "$BUILD_SUCCESS" != "true" ] && continue
              
              # Generate correct domain name
              SUBDOMAIN=$(echo "$TEMPLATE" | sed 's/-template$//')
              echo "$SUBDOMAIN" | grep -q "^preview-" || SUBDOMAIN="preview-$SUBDOMAIN"
              FULL_DOMAIN="$SUBDOMAIN.$MAIN_DOMAIN"
              
              echo "📝 Installing SSL for: $FULL_DOMAIN"
              
              # Try to install SSL (will work if DNS is correct)
              certbot --apache \
                -d "$FULL_DOMAIN" \
                --non-interactive \
                --agree-tos \
                --email "${{ secrets.SSL_EMAIL }}" \
                --redirect \
                --expand 2>&1 | grep -E "(Congratulations|already|ERROR)" || true
              
              sleep 2
            done

            echo "✅ SSL installation attempts completed"

               # Simple MIME check for all sites
            echo ""
            echo "🔍 Final MIME type verification..."
            for dir in /var/www/previews/preview-* 2>/dev/null; do
              SITE=$(basename "$dir")
              echo -n "Checking $SITE: "
              curl -s -I "https://$SITE.$MAIN_DOMAIN/assets/index-*.js" 2>/dev/null | \
                grep -i "content-type" | head -1 || echo "Check completed"
            done

            systemctl reload apache2 2>/dev/null || echo "Apache reload completed"

            rm -rf /tmp/deployment-package

            echo ""
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "🎉 Deployment Complete!"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

            echo ""
            echo "📋 Deployed sites:"
            ls -1 /var/www/previews/ 2>/dev/null | while read dir; do
              echo "  🌐 https://$dir.${{ secrets.MAIN_DOMAIN }}"
            done

      - name: Summary
        if: always()
        run: |
          echo ""
          echo "📊 Deployment Summary"
          echo "===================="
          echo "Templates: ${{ steps.strategy.outputs.templates }}"
          echo "Initial deployment: ${{ steps.strategy.outputs.is_initial }}"
